# =============================================================================
# Test-Specific Configuration
# =============================================================================
# This file contains test-specific data (credentials, test data, etc.).
# It is imported via spring.config.import in application.properties.
#
# Team members can add their own test properties here without modifying
# the framework-level application-qa.properties file.
# =============================================================================

# Test Credentials - Valid User
parabank.credentials.username=john
parabank.credentials.password=demo

# Test Credentials - Invalid User
parabank.credentials.invalid.username=invalidUser
parabank.credentials.invalid.password=wrongPassword
